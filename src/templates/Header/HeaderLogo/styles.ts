import styled from 'styled-components';

export const HeaderLogoContainer = styled.figure`
  img {
    display: block;
    width: 100%;

    @media (max-width: 390px) {
      max-width: 125px;
    }
  }
`;

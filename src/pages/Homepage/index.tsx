import React from 'react';
import { Header } from '../../templates/Header';

export const Homepage = (): JSX.Element => {
  return (
    <React.Fragment>
      <Header />
    </React.Fragment>
  );
};
